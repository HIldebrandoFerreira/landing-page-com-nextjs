import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;

// # Adionar logica no btn "Trocar agora": da tela inical e da tela de detalhes da oferta
//   - validar se a posse não é vivo total e se a oferta seleciona é VT , se for deve direcionar para a tela de desabiguição de linha
//     - logica
//     - Testes unitarios
//     -Testes de mutação
//   - Previsão 8hs

// # Organizar o fluxo de troca adicionado as telas novas
//   - Tela de desambuguação

// # novas telas
